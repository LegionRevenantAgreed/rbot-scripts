/*
	Created by LegionRevenantAgreed
	This script farms for Vampire Rep Rank 10.

	Notes:
		The script uses a private room.
*/

using RBot;
using System.Timers;

public class Script{

	public void ScriptMain(ScriptInterface bot){
		bot.Skills.StartTimer();
		bot.Options.SafeTimings = true;
		bot.Options.RestPackets = true;
		bot.Options.PrivateRooms = true;
		bot.Options.ExitCombatBeforeQuest = true;
		
		bot.Skills.LoadSkills("./Skills/Generic.xml");
				
		while (!bot.ShouldExit()) {
			bot.Quests.EnsureAccept(522);
			bot.Player.Join("Safiria");
			bot.Player.HuntForItem("Twisted Paw", "Twisted Paw's Head", 1, tempItem:true);
			bot.Quests.EnsureComplete(522);
		}
		
		bot.Exit();
	}
}
